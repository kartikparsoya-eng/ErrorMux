---
phase: 02-cli-ollama-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/errormux/client.py
  - src/errormux/parser.py
autonomous: true
requirements:
  - CLI-04
  - CLI-06
user_setup: []

must_haves:
  truths:
    - "Ollama client wrapper exists with 10s timeout"
    - "Client streams responses and buffers full text"
    - "Timeout prints '[explainer offline]' and exits 0"
    - "Service down prints '[explainer offline]' and exits 0"
    - "Response parser extracts WHY and FIX sections"
    - "Unparseable response prints raw output"
  artifacts:
    - path: "src/errormux/client.py"
      provides: "Ollama client wrapper with streaming"
      min_lines: 30
    - path: "src/errormux/parser.py"
      provides: "WHY/FIX response parsing"
      min_lines: 20
    - path: "pyproject.toml"
      contains: "ollama"
  key_links:
    - from: "src/errormux/client.py"
      to: "ollama.Client"
      via: "import ollama"
      pattern: "from ollama import Client"
    - from: "src/errormux/client.py"
      to: "localhost:11434"
      via: "Client(host=...)"
      pattern: "localhost:11434"
---

<objective>
Create Ollama client wrapper with streaming, timeout handling, and response parsing for WHY/FIX extraction.

Purpose: Provide a clean, testable interface to Ollama that handles errors gracefully per user decisions D-04 through D-10.
Output: client.py (Ollama wrapper), parser.py (response parsing)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cli-ollama-core/02-CONTEXT.md
@.planning/phases/02-cli-ollama-core/02-RESEARCH.md
@.planning/research/STACK.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ollama SDK (per RESEARCH.md):
```python
from ollama import Client
client = Client(host='http://localhost:11434', timeout=10.0)
# chat() returns ChatResponse or generator when stream=True
```

Expected module interfaces:
```python
# src/errormux/client.py
def chat_with_ollama(system_prompt: str, user_prompt: str) -> str:
    """Stream from Ollama, return buffered response."""
    
# src/errormux/parser.py  
def parse_response(response: str) -> tuple[str, str]:
    """Parse WHY and FIX from LLM response. Returns (why, fix)."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Ollama client wrapper with streaming and error handling</name>
  <files>
    - pyproject.toml
    - src/errormux/client.py
    - tests/test_client.py
  </files>
  <read_first>
    - pyproject.toml (current dependencies)
    - .planning/phases/02-cli-ollama-core/02-CONTEXT.md (decisions D-04, D-08, D-09)
    - .planning/phases/02-cli-ollama-core/02-RESEARCH.md (Ollama patterns)
    - .planning/research/STACK.md (ollama SDK usage)
  </read_first>
  <behavior>
    - Test 1: chat_with_ollama returns buffered response when Ollama responds
    - Test 2: chat_with_ollama raises TimeoutError after 10s timeout (per D-09)
    - Test 3: chat_with_ollama handles Ollama service down gracefully
    - Test 4: chat_with_ollama streams response and buffers full text (per D-04, D-05)
  </behavior>
  <action>
    1. Add ollama>=0.6.1 to pyproject.toml dependencies
       - Run: `uv add ollama`
    
    2. Create src/errormux/client.py with:
       - Import: `from ollama import Client`
       - Import: `from httpx import TimeoutException`
       - Constant: `OLLAMA_HOST = "http://localhost:11434"`
       - Constant: `OLLAMA_TIMEOUT = 10.0` (per D-09)
       - Constant: `OLLAMA_MODEL = "gemma3:4b"` (per project constraints)
       - Function: `chat_with_ollama(system_prompt: str, user_prompt: str) -> str`
         - Create Client with host and timeout
         - Call client.chat() with model, messages, stream=True (per D-04)
         - Iterate over chunks, buffer into response_text (per D-05)
         - Return buffered response
         - Catch TimeoutException and re-raise as TimeoutError
         - Catch ollama.ResponseError and re-raise as ConnectionError
    
    3. Create tests/test_client.py with:
       - Mock ollama.Client.chat to return streaming generator
       - Mock timeout by raising httpx.TimeoutException
       - Mock service down by raising ollama.ResponseError
       - Test that streaming chunks are buffered correctly
  </action>
  <verify>
    <automated>uv run pytest tests/test_client.py -xvs</automated>
  </verify>
  <done>
    - pyproject.toml contains "ollama" in dependencies
    - src/errormux/client.py exists with chat_with_ollama function
    - tests/test_client.py exists and passes
    - chat_with_ollama streams and buffers response
    - TimeoutException is caught and re-raised as TimeoutError
    - All tests pass: `uv run pytest tests/test_client.py -xvs`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create response parser for WHY/FIX extraction</name>
  <files>
    - src/errormux/parser.py
    - tests/test_parser.py
  </files>
  <read_first>
    - .planning/phases/02-cli-ollama-core/02-CONTEXT.md (decisions D-02, D-06, D-10)
    - .planning/phases/02-cli-ollama-core/02-RESEARCH.md (response parsing patterns)
  </read_first>
  <behavior>
    - Test 1: parse_response extracts "WHY: foo\nFIX: bar" correctly
    - Test 2: parse_response handles multi-line WHY text
    - Test 3: parse_response raises ParseError for malformed response
    - Test 4: Unparseable response returns raw text (per D-10)
  </behavior>
  <action>
    1. Create src/errormux/parser.py with:
       - Import: `import re`
       - Custom exception: `class ParseError(Exception): pass`
       - Function: `parse_response(response: str) -> tuple[str, str]`
         - Use regex to extract WHY: and FIX: sections
         - Pattern: `r'WHY:\s*(.+?)(?=\nFIX:|$)'` for WHY
         - Pattern: `r'FIX:\s*(.+?)$'` for FIX
         - Strip whitespace from both
         - If either not found, raise ParseError
       - Function: `format_output(why: str, fix: str) -> str`
         - Return formatted string for fallback display
    
    2. Create tests/test_parser.py with:
       - Test standard format: "WHY: File not found.\nFIX: touch myfile.txt"
       - Test multi-line WHY: "WHY: The file doesn't\nexist.\nFIX: touch file"
       - Test malformed: "Some random text without labels" -> raises ParseError
       - Test edge: extra whitespace, lowercase labels
  </action>
  <verify>
    <automated>uv run pytest tests/test_parser.py -xvs</automated>
  </verify>
  <done>
    - src/errormux/parser.py exists with parse_response function
    - tests/test_parser.py exists and passes
    - parse_response correctly extracts WHY and FIX
    - ParseError raised for malformed responses
    - All tests pass: `uv run pytest tests/test_parser.py -xvs`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Ollama | Local trusted service at localhost:11434 |
| Ollama → CLI | LLM output, parse safely without shell eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | E (Elevation of Privilege) | parser.py | mitigate | Never execute FIX command, only display as text. User must manually run. |
| T-02-02 | T (Tampering) | client.py | accept | Local service, no auth needed. Single-user machine. |
| T-02-03 | I (Information Disclosure) | client.py | accept | Temp files in /tmp are world-readable, but single-user machine. |
</threat_model>

<verification>
- [ ] ollama dependency added to pyproject.toml
- [ ] client.py streams and buffers Ollama responses
- [ ] client.py handles timeout (10s) gracefully
- [ ] client.py handles service down gracefully
- [ ] parser.py extracts WHY and FIX sections
- [ ] parser.py handles malformed responses
- [ ] All tests pass
</verification>

<success_criteria>
- Ollama client wrapper exists with 10s timeout (D-09)
- Responses are streamed and buffered (D-04, D-05)
- Errors result in graceful exit with "[explainer offline]" (D-08)
- Response parser extracts WHY/FIX (D-02)
- Unparseable responses print raw output (D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-ollama-core/02-01-SUMMARY.md`
</output>
