---
phase: 04-skip-list-filtering
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/errormux/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [SKIP-01, SKIP-02, SKIP-03, SKIP-04]
must_haves:
  truths:
    - "Running `errormux explain` on captured `grep foo file` with exit 1 prints ONE dim notice and exits without calling Ollama or writing cache (D-10, SKIP-01)"
    - "Same for test/[[/[ exit 1 (SKIP-02) and diff exit 1 (SKIP-03)"
    - "Running `errormux explain -f` (or --force) bypasses skip list and runs full explain flow (D-11)"
    - "Skip check runs BEFORE cache_get and BEFORE chat_with_ollama (D-03, D-04)"
    - "On skipped command, no cache_set is called (D-10)"
    - "Real errors (e.g., grep exit 2) still flow through normal explain path"
  artifacts:
    - path: "src/errormux/cli.py"
      provides: "CLI with skip-check short-circuit + --force flag + dim skip notice"
      contains: "should_skip"
    - path: "tests/test_cli.py"
      provides: "Tests for skip short-circuit, force bypass, notice format, no cache write on skip"
  key_links:
    - from: "src/errormux/cli.py"
      to: "errormux.skip.should_skip"
      via: "called immediately after read_context, before cache_get"
      pattern: "should_skip\\("
    - from: "src/errormux/cli.py"
      to: "extract_command_name"
      via: "parses captured cmd to get command_name for skip check"
      pattern: "extract_command_name\\("
---

<objective>
Wire the skip module into the CLI so skipped commands short-circuit before cache/LLM work. Add a `--force` (`-f`) flag to bypass skipping. Print a single dim notice on skip.

Purpose: Deliver end-to-end SKIP-01..04 behavior to users.
Output: Updated `src/errormux/cli.py` + CLI-level tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-skip-list-filtering/04-CONTEXT.md
@.planning/phases/04-skip-list-filtering/04-01-PLAN.md
@src/errormux/cli.py
@src/errormux/skip.py
@tests/test_cli.py

<interfaces>
Plan 01 exports from errormux.skip:
- `should_skip(command_name: str, exit_code: int) -> bool`
- `extract_command_name(command_line: str) -> str`

Existing CLI (src/errormux/cli.py) currently flows:
  read_context → make_cache_key → cache_get → (hit: print, return) → chat_with_ollama → parse → print → cache_set

Target flow (post-change):
  read_context → [skip check unless --force] → make_cache_key → cache_get → ... (unchanged)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate skip check + --force flag into explain command</name>
  <files>src/errormux/cli.py</files>
  <behavior>
    - explain() accepts a `--force / -f` boolean option (default False).
    - After read_context(), if not force: compute `cmd_name = extract_command_name(cmd)` and check `should_skip(cmd_name, exit_code)`. If True → print dim notice `not an error ({cmd_name} exit {exit_code}) — nothing to explain` and return. No cache_get, no LLM, no cache_set (D-03/D-04/D-10).
    - If force=True, skip check is bypassed entirely; rest of flow unchanged (D-11).
    - Real errors (exit_code not in skip rules) continue to cache+LLM flow as before.
  </behavior>
  <action>
    Edit src/errormux/cli.py:

    1. Add imports: `from errormux.skip import extract_command_name, should_skip`.

    2. Change explain() signature:
       ```python
       @app.command()
       def explain(
           force: bool = typer.Option(False, "--force", "-f", help="Bypass skip list and force LLM explanation"),
       ) -> None:
       ```

    3. After `cmd, stderr, exit_code = read_context()` (and its try/except), add:
       ```python
       if not force:
           cmd_name = extract_command_name(cmd)
           if should_skip(cmd_name, exit_code):
               console.print(
                   f"not an error ({cmd_name} exit {exit_code}) — nothing to explain",
                   style="dim",
               )
               return
       ```
       This MUST execute before `make_cache_key` / `cache_get` (D-04).

    4. Leave the rest of explain() untouched.

    Do NOT change prompts, client, parser, or cache modules. Keep the dim style consistent with existing Rich usage (style="dim").
  </action>
  <verify>
    <automated>uv run python -c "from errormux.cli import app; import typer.testing; r = typer.testing.CliRunner().invoke(app, ['explain','--help']); assert '--force' in r.output and '-f' in r.output, r.output"</automated>
  </verify>
  <done>explain() has --force/-f flag; skip check runs post-read_context and pre-cache; dim notice printed on skip; no cache or LLM calls on skipped path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI tests for skip short-circuit, force bypass, and no-cache-on-skip</name>
  <files>tests/test_cli.py</files>
  <behavior>
    - test_explain_skips_grep_exit_1: temp files set to `grep foo file` / stderr / exit 1 → explain() prints dim notice containing "grep exit 1", does NOT call chat_with_ollama, does NOT call cache_set.
    - test_explain_skips_diff_exit_1: same for `diff a b` / exit 1.
    - test_explain_skips_test_exit_1: same for `test -f x` / exit 1.
    - test_explain_force_bypasses_skip: same captured grep scenario but invoked with `--force` → chat_with_ollama IS called; normal flow runs.
    - test_explain_real_error_not_skipped: `grep` with exit_code=2 → normal cache/LLM path runs (not skipped).
    - test_skip_notice_format: output contains "not an error" and "nothing to explain".
  </behavior>
  <action>
    Extend tests/test_cli.py (mirror existing patterns there). Use:
    - `typer.testing.CliRunner` to invoke the app.
    - `monkeypatch` to point TEMP_CMD / TEMP_STDERR / TEMP_EXIT at tmp_path files (or monkeypatch `errormux.cli.read_context` directly — simpler).
    - `monkeypatch.setattr("errormux.cli.chat_with_ollama", mock)` to assert called/not-called.
    - `monkeypatch.setattr("errormux.cli.cache_set", mock)` to assert not called on skip.
    - `monkeypatch.setattr("errormux.cli.cache_get", lambda _k: None)` to keep cache miss path clean.
    - For force-bypass test, mock chat_with_ollama to return a valid "WHY: x\nFIX: y" response so parse_response succeeds.

    Run existing tests to ensure no regressions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_skip.py -v</automated>
  </verify>
  <done>All six new CLI tests pass; existing CLI tests still pass; skip module tests still pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest -v` — full suite green (cache, client, parser, prompts, skip, cli).
- Manual smoke (optional, not a gate): write temp files for `grep x nonexistent` exit 1, run `uv run errormux explain` → see dim notice; run with `-f` → LLM is invoked (will print `[explainer offline]` if Ollama down, which is fine).
- `grep -n "should_skip" src/errormux/cli.py` shows skip check placed before `make_cache_key`.
</verification>

<success_criteria>
- SKIP-01 (grep), SKIP-02 (test/[[/[), SKIP-03 (diff) all short-circuit with dim notice — no LLM, no cache write.
- SKIP-04: user config additions/disables honored end-to-end (covered via Plan 01 module tests; CLI uses the same loader).
- `--force`/`-f` bypasses skip list (D-11).
- Skip check position: after read_context, before cache_get (D-03, D-04).
- No regressions in existing Phase 2/3 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-skip-list-filtering/04-02-SUMMARY.md`
</output>
