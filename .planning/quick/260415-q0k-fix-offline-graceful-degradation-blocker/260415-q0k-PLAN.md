---
mode: quick
quick_id: 260415-q0k
description: Fix offline graceful degradation blocker in Phase 2 UAT - add ConnectError exception handler
date: 2026-04-15
---

# Quick Task: Fix Offline Graceful Degradation

## Objective
Add `httpx.ConnectError` exception handling to `src/errormux/client.py` so the CLI shows "[explainer offline]" instead of crashing when Ollama is unavailable.

## Context
- CLI already handles `ConnectionError` gracefully (lines 100-101 in cli.py)
- client.py handles `TimeoutException` and `ollama.ResponseError`
- Missing: `httpx.ConnectError` (raised when connection fails to localhost:11434)

## Tasks

### Task 1: Add ConnectError exception handler
**type:** auto
**files:** src/errormux/client.py
**action:**
1. Add `ConnectError` to the httpx import (line 3)
2. Add exception handler for `ConnectError` after the `TimeoutException` handler
3. Raise `ConnectionError` with message "Ollama service unavailable"

**verify:**
- Import statement includes `ConnectError`
- Exception handler catches `ConnectError` and raises `ConnectionError`

**done:**
- Code compiles without errors
- Exception handling follows existing pattern
