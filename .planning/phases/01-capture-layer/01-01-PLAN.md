---
phase: 01-capture-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/errormux/__init__.py
  - src/errormux/cli.py
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - "User can run `errormux explain` from command line"
    - "CLI prints placeholder message indicating Phase 2 implementation"
  artifacts:
    - path: pyproject.toml
      provides: "Python project configuration with uv"
      contains: "[project]"
    - path: src/errormux/__init__.py
      provides: "Python package initialization"
      min_lines: 1
    - path: src/errormux/cli.py
      provides: "Stub CLI entry point"
      contains: "def explain"
  key_links:
    - from: pyproject.toml
      to: "src/errormux.cli:app"
      via: "script entry point"
      pattern: "errormux\\s*=\\s*\"errormux.cli:app\""
---

<objective>
Create Python project structure with stub CLI that prints placeholder message.

Purpose: Establish project foundation and CLI entry point for zsh widget to invoke.
Output: Working `errormux explain` command that prints placeholder (per D-05, D-06).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-layer/01-CONTEXT.md
@.planning/phases/01-capture-layer/01-RESEARCH.md

<interfaces>
<!-- Key patterns from RESEARCH.md for executor reference -->

Python project structure (from RESEARCH.md):
```
errormux/
├── pyproject.toml           # Python project config (uv)
├── src/
│   └── errormux/
│       ├── __init__.py
│       └── cli.py           # typer CLI entry point
```

Stub CLI pattern (from RESEARCH.md lines 349-363):
```python
import typer

app = typer.Typer()

@app.command()
def explain():
    """Stub CLI - prints placeholder message."""
    typer.echo("[errormux] CLI not implemented yet - coming in Phase 2")

if __name__ == "__main__":
    app()
```

pyproject.toml structure (from STACK.md):
```toml
[project]
name = "errormux"
version = "0.1.0"
dependencies = ["typer>=0.15", "rich>=14.0"]

[project.scripts]
errormux = "errormux.cli:app"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project configuration</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/research/STACK.md (for pyproject.toml pattern and dependencies)
    - .planning/phases/01-capture-layer/01-RESEARCH.md (for project structure)
  </read_first>
  <action>Create pyproject.toml with the following content:

```toml
[project]
name = "errormux"
version = "0.1.0"
description = "zsh plugin for on-demand shell command error explanations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.15",
    "rich>=14.0",
]

[project.scripts]
errormux = "errormux.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/errormux"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
```

This configuration:
- Declares typer and rich as dependencies (per STACK.md)
- Creates `errormux` script entry point pointing to `errormux.cli:app`
- Uses hatchling as build backend (uv-compatible)
- Sets Python 3.12+ requirement (per project constraints)</action>
  <verify>
    <automated>test -f pyproject.toml && grep -q 'name = "errormux"' pyproject.toml && grep -q 'errormux = "errormux.cli:app"' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at project root
    - pyproject.toml contains `[project]` section with `name = "errormux"`
    - pyproject.toml contains `errormux = "errormux.cli:app"` in `[project.scripts]`
    - pyproject.toml lists `typer>=0.15` and `rich>=14.0` as dependencies
    - pyproject.toml specifies `requires-python = ">=3.12"`
  </acceptance_criteria>
  <done>pyproject.toml exists with errormux CLI entry point configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create Python package structure</name>
  <files>src/errormux/__init__.py, src/errormux/cli.py</files>
  <read_first>
    - pyproject.toml (created in Task 1, to verify entry point path)
    - .planning/phases/01-capture-layer/01-RESEARCH.md (for stub CLI pattern)
  </read_first>
  <action>Create the Python package structure:

1. Create directory: `src/errormux/`

2. Create `src/errormux/__init__.py` with:
```python
"""ErrorMux - zsh plugin for on-demand shell command error explanations."""

__version__ = "0.1.0"
```

3. Create `src/errormux/cli.py` with stub CLI (per D-05):
```python
"""CLI entry point for ErrorMux."""

import typer

app = typer.Typer(
    name="errormux",
    help="On-demand shell command error explanations",
)


@app.command()
def explain() -> None:
    """Explain the last failed command.

    Stub implementation - real functionality coming in Phase 2.
    """
    typer.echo("[errormux] CLI not implemented yet - coming in Phase 2")


if __name__ == "__main__":
    app()
```

This creates a typer CLI with a single `explain` command that prints the placeholder message per D-05.</action>
  <verify>
    <automated>test -f src/errormux/__init__.py && test -f src/errormux/cli.py && grep -q 'def explain' src/errormux/cli.py && grep -q 'CLI not implemented yet' src/errormux/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - src/errormux/__init__.py exists and contains `__version__`
    - src/errormux/cli.py exists and contains `app = typer.Typer()`
    - src/errormux/cli.py contains `def explain()` function decorated with `@app.command()`
    - src/errormux/cli.py contains placeholder message "CLI not implemented yet - coming in Phase 2"
    - src/errormux/cli.py contains `if __name__ == "__main__": app()` block
  </acceptance_criteria>
  <done>Python package created with stub CLI that prints placeholder message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CLI | Local CLI invocation, no external input in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | cli.py output | accept | Stub only prints static placeholder - no user data exposed |
</threat_model>

<verification>
After all tasks complete:
1. Run `uv sync` to install dependencies
2. Run `uv run errormux explain` to verify CLI works
3. Verify output contains "CLI not implemented yet"
</verification>

<success_criteria>
- pyproject.toml exists with errormux entry point
- src/errormux/ package structure exists
- `uv run errormux explain` prints placeholder message
- No import errors when running CLI
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-layer/01-01-SUMMARY.md`
</output>
