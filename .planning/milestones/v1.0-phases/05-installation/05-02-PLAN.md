---
phase: 05-installation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: []
autonomous: false
requirements: [INST-01, INST-02, INST-03]
user_setup: []

must_haves:
  truths:
    - "User can run install.sh successfully in a clean environment"
    - "After installation, opening new shell activates the plugin"
    - "User can trigger ?? after a failed command"
  artifacts:
    - path: "~/.shell-explainer/errormux.plugin.zsh"
      provides: "Installed plugin file"
    - path: "~/.shell-explainer/"
      provides: "Python environment with uv"
  key_links:
    - from: "New shell"
      to: "Plugin active"
      via: ".zshrc source"
      pattern: "_ERRORMUX_"
---

<objective>
Verify the installation flow works end-to-end for a real user.

Purpose: Confirm install.sh produces a working ErrorMux setup.
Output: Verified installation with user confirmation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-installation/05-CONTEXT.md
@.planning/phases/05-installation/05-01-PLAN.md

## Installation Requirements (from ROADMAP)

1. Running install.sh copies plugin to ~/.shell-explainer/
2. .zshrc is updated with source line if missing (no duplicate entries)
3. Python dependencies are installed via uv automatically

## Verification Steps

1. Ensure .zshrc contains the source line
2. Open a new shell (or source ~/.zshrc)
3. Verify plugin functions are defined (_ERRORMUX_* variables)
4. Run a failing command and trigger ?? (should show explanation)

</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify end-to-end installation flow</name>
  <files></files>
  <action>
Verify install.sh works end-to-end by testing the complete installation flow:

**What was built:**
- install.sh script that clones repo to ~/.shell-explainer/
- Updates .zshrc with source line (no duplicates)
- Installs Python dependencies via uv sync
- Shows success message with next steps

**Verification steps:**
1. Pre-cleanup (optional): `rm -rf ~/.shell-explainer` and remove source line from .zshrc
2. Run install.sh from repo root
3. Verify file structure: `ls -la ~/.shell-explainer/`
4. Verify .zshrc: `grep "shell-explainer" ~/.zshrc`
5. Open new shell and test: `echo $_ERRORMUX_STDERR_FILE` should show path
6. Test with failing command: `ls /nonexistent` then `??`
7. Test idempotency: run install.sh again, verify no duplicates
  </action>
  <verify>
    <automated>echo "Human verification required - see verification steps"</automated>
  </verify>
  <done>User confirms all verification steps pass: files installed, .zshrc updated, plugin active, ?? works</done>
  <what-built>
install.sh script that:
- Clones errormux repo to ~/.shell-explainer/
- Updates .zshrc with source line (no duplicates)
- Installs Python dependencies via uv sync
- Shows success message with next steps
  </what-built>
  <how-to-verify>
**Step 1: Pre-cleanup (if testing fresh install)**
```bash
# Optional: clean slate for testing
rm -rf ~/.shell-explainer
# Remove source line from .zshrc if it exists
sed -i.bak '/shell-explainer/d' ~/.zshrc && rm -f ~/.zshrc.bak
```

**Step 2: Run install.sh**
```bash
# From repo root
./install.sh
```

**Step 3: Verify file structure**
```bash
ls -la ~/.shell-explainer/
# Should show: errormux.plugin.zsh, pyproject.toml, src/, .git/
```

**Step 4: Verify .zshrc modification**
```bash
grep "shell-explainer" ~/.zshrc
# Should show: source ~/.shell-explainer/errormux.plugin.zsh
```

**Step 5: Open new shell and test plugin**
```bash
# Open new terminal or run:
source ~/.zshrc

# Verify plugin loaded (check for plugin functions)
echo $_ERRORMUX_STDERR_FILE
# Should output: /tmp/shell-explainer-last-stderr

# Test with a failing command
ls /nonexistent

# Trigger explanation
??
# Should show: [skipped] message (since grep/test/diff exit 1 are filtered)
# Or: explanation from Ollama if it's a real error
```

**Step 6: Test idempotency**
```bash
# Run install.sh again
./install.sh
# Should show "Updating existing installation..." and ".zshrc already configured"
# No duplicate source lines in .zshrc
grep -c "shell-explainer" ~/.zshrc
# Should be 1-2 lines (comment + source), not duplicated
```
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User verification | Manual testing confirms expected behavior |

## STRIDE Threat Register

(No additional threats — verification is passive observation)
</threat_model>

<verification>
Human verification checkpoint confirms:
1. install.sh runs without errors
2. Files installed to correct locations
3. .zshrc has single source line (no duplicates)
4. Plugin activates in new shell
5. ?? works after failed command
</verification>

<success_criteria>
- [ ] install.sh runs successfully
- [ ] ~/.shell-explainer/errormux.plugin.zsh exists
- [ ] ~/.zshrc contains source line (single entry)
- [ ] New shell activates plugin (_ERRORMUX_* variables present)
- [ ] ?? triggers explanation after failed command
- [ ] Running install.sh twice is idempotent (no duplicates)
</success_criteria>

<output>
After completion, create `.planning/phases/05-installation/05-02-SUMMARY.md`
</output>
