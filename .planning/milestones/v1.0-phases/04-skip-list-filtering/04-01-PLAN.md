---
phase: 04-skip-list-filtering
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/errormux/skip.py
  - tests/test_skip.py
autonomous: true
requirements: [SKIP-01, SKIP-02, SKIP-03, SKIP-04]
must_haves:
  truths:
    - "skip.should_skip('grep', 1) returns True (SKIP-01, D-02)"
    - "skip.should_skip('test', 1) returns True and '[[' / '[' also True (SKIP-02, D-02)"
    - "skip.should_skip('diff', 1) returns True (SKIP-03, D-02)"
    - "skip.should_skip('grep', 2) returns False (real errors pass through, D-02)"
    - "skip.should_skip('ls', 1) returns False (not in skip list)"
    - "User config at ~/.shell-explainer/config.toml can add entries and disable built-ins (SKIP-04, D-06/D-07)"
    - "Missing or malformed config silently falls back to built-ins (D-09)"
    - "extract_command_name('FOO=bar grep x') returns 'grep' (D-01, best-effort env strip)"
  artifacts:
    - path: "src/errormux/skip.py"
      provides: "SkipRule loading + should_skip(command_name, exit_code) predicate + extract_command_name()"
      exports: ["should_skip", "load_skip_rules", "extract_command_name", "BUILTIN_SKIP_RULES"]
    - path: "tests/test_skip.py"
      provides: "Unit tests covering built-ins, user add, user disable, malformed config, command-name extraction"
  key_links:
    - from: "src/errormux/skip.py"
      to: "tomllib (stdlib)"
      via: "load_skip_rules reads ~/.shell-explainer/config.toml"
      pattern: "tomllib\\.load"
    - from: "src/errormux/skip.py"
      to: "BUILTIN_SKIP_RULES"
      via: "merged with user config (additive, with disable list)"
      pattern: "BUILTIN_SKIP_RULES"
---

<objective>
Build the skip-list module: built-in defaults, TOML config loader, and the core `should_skip(command_name, exit_code)` predicate. Pure logic with no side effects on the CLI — Plan 02 wires it in.

Purpose: Prevent noise from semantically-expected non-zero exits (grep no-match, test false, diff different). Closes SKIP-01..04.
Output: `src/errormux/skip.py` module + passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-skip-list-filtering/04-CONTEXT.md
@src/errormux/cache.py
@src/errormux/cli.py

<interfaces>
<!-- Existing module pattern from cache.py — mirror the style -->

From src/errormux/cache.py (module-level functions, pathlib.Path, typed returns).
Tests live in tests/test_<module>.py and use pytest fixtures with tmp_path.

This plan creates (consumed by Plan 02):

```python
# src/errormux/skip.py

BUILTIN_SKIP_RULES: dict[str, set[int]] = {
    "grep": {1},
    "test": {1},
    "[[": {1},
    "[": {1},
    "diff": {1},
}

def extract_command_name(command_line: str) -> str:
    """First non-assignment token. 'FOO=bar grep x' -> 'grep'."""

def load_skip_rules(config_path: Path | None = None) -> dict[str, set[int]]:
    """Merge built-ins with user TOML config. Missing/malformed -> built-ins only.
    User config shape (D-07/D-08):
        [skip]
        disable = ["grep"]          # list of builtin names to drop
        [[skip.rules]]              # additive entries
        command = "make"
        exit_code = 2
    """

def should_skip(command_name: str, exit_code: int, rules: dict[str, set[int]] | None = None) -> bool:
    """True if (command_name, exit_code) matches a skip rule. Lazy-loads rules."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for skip module</name>
  <files>tests/test_skip.py</files>
  <behavior>
    - test_builtin_grep_exit_1_skipped: should_skip("grep", 1) is True
    - test_builtin_grep_exit_2_not_skipped: should_skip("grep", 2) is False (real errors pass)
    - test_builtin_test_and_bracket_skipped: "test"/"[["/"[" exit 1 all True
    - test_builtin_diff_exit_1_skipped: should_skip("diff", 1) is True
    - test_unknown_command_not_skipped: should_skip("ls", 1) is False
    - test_extract_command_name_simple: "grep foo bar" -> "grep"
    - test_extract_command_name_env_prefix: "FOO=bar BAZ=1 grep x" -> "grep"
    - test_extract_command_name_empty: "" -> ""
    - test_user_config_add_rule: TOML with `[[skip.rules]] command="make" exit_code=2` -> should_skip("make", 2) True
    - test_user_config_disable_builtin: TOML with `[skip] disable=["grep"]` -> should_skip("grep", 1) False
    - test_missing_config_falls_back: load_skip_rules(nonexistent_path) returns BUILTIN_SKIP_RULES
    - test_malformed_config_falls_back: load_skip_rules(garbled_toml) returns BUILTIN_SKIP_RULES, no exception
    Use tmp_path fixture to write config files.
  </behavior>
  <action>
    Create tests/test_skip.py. Import from errormux.skip (module will not exist yet — tests MUST fail with ImportError on first run). Follow pytest style used in tests/test_cache.py. Use tmp_path for config files; pass config_path explicitly to load_skip_rules (no monkeypatching HOME required).
  </action>
  <verify>
    <automated>uv run pytest tests/test_skip.py -x 2>&1 | grep -E "(ImportError|ModuleNotFoundError|failed)"</automated>
  </verify>
  <done>All tests written; running pytest fails with ImportError for errormux.skip (RED phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement skip.py to make tests pass</name>
  <files>src/errormux/skip.py</files>
  <behavior>
    Implements BUILTIN_SKIP_RULES, extract_command_name, load_skip_rules, should_skip as specified in &lt;interfaces&gt;. Makes all tests from Task 1 pass (GREEN phase).
  </behavior>
  <action>
    Create src/errormux/skip.py:

    1. Define BUILTIN_SKIP_RULES = {"grep": {1}, "test": {1}, "[[": {1}, "[": {1}, "diff": {1}} (per D-02).

    2. `extract_command_name(command_line: str) -> str` (per D-01):
       - Split on whitespace; iterate tokens; skip tokens matching `^[A-Za-z_][A-Za-z0-9_]*=` (env assignments); return first non-assignment token, or "" if none.

    3. `load_skip_rules(config_path: Path | None = None) -> dict[str, set[int]]`:
       - Default config_path = Path.home() / ".shell-explainer" / "config.toml".
       - Start with `rules = {k: set(v) for k, v in BUILTIN_SKIP_RULES.items()}` (deep copy).
       - If config_path missing → return built-ins (D-09).
       - Try `tomllib.load(open(path, "rb"))` inside try/except (OSError, tomllib.TOMLDecodeError) → on failure return built-ins silently (D-09).
       - Apply `skip.disable` list: for name in disable, `rules.pop(name, None)` (D-07).
       - Apply `skip.rules` array of tables: for entry in rules_list, `rules.setdefault(entry["command"], set()).add(int(entry["exit_code"]))` (D-08, single codes only).
       - Guard each merge step with try/except so one bad entry doesn't break the rest.
       - Return rules.

    4. `should_skip(command_name: str, exit_code: int, rules: dict[str, set[int]] | None = None) -> bool`:
       - If rules is None, call load_skip_rules() (cache at module level via functools.lru_cache-wrapped helper OR just load each call — pick the simpler path; load is cheap and test isolation matters more).
       - Return `exit_code in rules.get(command_name, set())`.

    Use stdlib only: `tomllib`, `pathlib`, `typing`. No new deps per CONTEXT D-07 (stdlib tomllib requirement).
  </action>
  <verify>
    <automated>uv run pytest tests/test_skip.py -x -v</automated>
  </verify>
  <done>All tests in tests/test_skip.py pass. Module exports should_skip, load_skip_rules, extract_command_name, BUILTIN_SKIP_RULES.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_skip.py -v` — all tests green.
- Module imports cleanly: `uv run python -c "from errormux.skip import should_skip, load_skip_rules, extract_command_name"`.
- No new dependencies added to pyproject.toml.
</verification>

<success_criteria>
- Built-ins match D-02 exactly (grep, test, [[, [, diff all exit 1).
- User TOML config can add rules AND disable built-ins (D-06, D-07, D-08).
- Malformed/missing config degrades silently to built-ins (D-09).
- Command-name extraction strips trivial env-var prefixes (D-01).
- All SKIP-01..04 requirements have module-level coverage (wiring in Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/04-skip-list-filtering/04-01-SUMMARY.md`
</output>
